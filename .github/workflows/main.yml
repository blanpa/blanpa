name: Generate Stats Images

on:
  push:
  schedule:
    - cron: "5 0 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Set up Python
      uses: actions/setup-python@v7
      with:
        python-version: '3.12'
        cache: 'pip'

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Generate images
      run: python generate_images.py
      env:
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        GITHUB_ACTOR: ${{ github.actor }}
        EXCLUDED: ${{ secrets.EXCLUDED }}
        # Notebooks count their whole JSON payload and Roff is almost
        # always vendored man pages, so both drown out real work.
        EXCLUDED_LANGS: ${{ secrets.EXCLUDED_LANGS || 'Jupyter Notebook,Roff' }}
        EXCLUDE_FORKED_REPOS: false

    - name: Commit changes
      uses: stefanzweifel/git-auto-commit-action@v7
      with:
        commit_message: "Update generated files"
        file_pattern: "generated/*.svg"
